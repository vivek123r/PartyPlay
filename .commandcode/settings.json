{
  "permissions": {
    "allow": [
      "config",
      "Shell(cat ~/.commandcode/settings.json 2>/dev/null; echo \"---\"; cat .commandcode/settings.json 2>/dev/null || echo \"No project settings\")",
      "Shell(npx tsc --noEmit --project tsconfig.app.json 2 >& 1)",
      "Shell(npx vite build 2 >& 1)",
      "Shell(cat << 'DEBUG' | node - // Simulate the projection formula for a 2-player viewport const CAMERA_DEPTH = 0.55; const CAMERA_HEIGHT = 400; // current value const ROAD_WIDTH = 2000; const viewW = 480; const viewH = 135; // 270/2 const horizonY = Math.round(viewH * 0.35); // 47 function project(worldX, worldY, worldZ, cameraX, cameraY, cameraZ) { const camX = worldX - cameraX; const camY = worldY - cameraY; const camZ = worldZ - cameraZ; const scale = CAMERA_DEPTH / Math.max(1, camZ); const screenX = Math.round(viewW / 2 + (scale * camX * viewW) / 2); const screenY = Math.round(horizonY - (scale * camY * viewH) / 2); const projectedWidth = Math.round((scale * ROAD_WIDTH * viewW) / 2); return { screenX, screenY, projectedWidth, scale, cameraZ: camZ }; } // Straight flat track, camera at z=20 (mid-segment), center const camX = 0, camY = CAMERA_HEIGHT, camZ = 20; console.log(`horizonY=${horizonY}, viewH=${viewH}`); // Far segment at 4800m const farP1 = project(0, 0, 4800, camX, camY, camZ); const farP2 = project(0, 0, 4840, camX, camY, camZ); console.log(`FAR d=4780: screenY=${farP1.screenY} scale=${farP1.scale.toFixed(6)}`); console.log(`FAR d=4820: screenY=${farP2.screenY} scale=${farP2.scale.toFixed(6)}`); console.log(` y1(<=)y2 = ${farP1.screenY} <= ${farP2.screenY} = ${farP1.screenY <= farP2.screenY}`); // Near segment at 20m const nearP1 = project(0, 0, 0, camX, camY, camZ); const nearP2 = project(0, 0, 40, camX, camY, camZ); console.log(`NEAR d=-20: screenY=${nearP1.screenY} scale=${nearP1.scale.toFixed(6)} cameraZ=${nearP1.cameraZ} ${nearP1.cameraZ <= 0 ? 'BEHIND!' : 'ahead'}`); console.log(`NEAR d=20: screenY=${nearP2.screenY} scale=${nearP2.scale.toFixed(6)} cameraZ=${nearP2.cameraZ}`); console.log(` y1(<=)y2 = ${nearP1.screenY} <= ${nearP2.screenY} = ${nearP1.screenY <= nearP2.screenY}`); // Mid segment at 500m const midP1 = project(0, 0, 480, camX, camY, camZ); const midP2 = project(0, 0, 520, camX, camY, camZ); console.log(`MID d=460: screenY=${midP1.screenY} scale=${midP1.scale.toFixed(6)}`); console.log(`MID d=500: screenY=${midP2.screenY} scale=${midP2.scale.toFixed(6)}`); console.log(` y1(<=)y2 = ${midP1.screenY} <= ${midP2.screenY} = ${midP1.screenY <= midP2.screenY}`); // Check what CAMERA_HEIGHT would work for (const ch of [40, 50, 60, 80, 100]) { const p = project(0, 0, 40, 0, ch, 20); const f = project(0, 0, 4800, 0, ch, 20); console.log(`CAMERA_HEIGHT=${ch}: near=screenY(${p.screenY}) far=screenY(${f.screenY})`); } DEBUG)",
      "Shell(cat << 'DEBUG' | node - const CAMERA_DEPTH = 0.55; const CAMERA_HEIGHT = 400; const ROAD_WIDTH = 2000; const NEAR_PLANE = 0.5; const viewW = 480; const viewH = 135; const horizonY = Math.round(viewH * 0.35); function project(worldX, worldY, worldZ, cameraX, cameraY, cameraZ) { const camX = worldX - cameraX; const camY = worldY - cameraY; const camZ = worldZ - cameraZ; const safeZ = Math.max(NEAR_PLANE, camZ); const scale = CAMERA_DEPTH / safeZ; const screenX = Math.round(viewW / 2 + (scale * camX * viewW) / 2); const screenY = Math.round(horizonY - (scale * camY * viewH) / 2); const projectedWidth = Math.round((scale * ROAD_WIDTH * viewW) / 2); return { screenX, screenY, projectedWidth, scale, cameraZ: camZ }; } const camX = 0; const camY = CAMERA_HEIGHT; const camZ = 0; console.log(`horizonY=${horizonY}, viewH=${viewH}, camY=${camY}`); // First segments at z=0 for (let n = 0; n < 5; n++) { const z1 = n * 40; const z2 = (n+1) * 40; const p1 = project(0, 0, z1, camX, camY, camZ); const p2 = project(0, 0, z2, camX, camY, camZ); const skipP1 = p1.cameraZ <= 1.0; const skipP2 = p2.cameraZ <= 0.55; const yFlip = p1.screenY <= p2.screenY; console.log(`n=${n} z=${z1}-${z2} | p1Z=${p1.cameraZ} p2Z=${p2.cameraZ} | y1=${p1.screenY} y2=${p2.screenY} | w1=${p1.projectedWidth} w2=${p2.projectedWidth} | skip=${skipP1||skipP2} yFlip=${yFlip}`); } DEBUG)",
      "Shell(awk:*)",
      "Shell(xxd:*)",
      "Shell(node -e  const fs = require('fs'); const content = fs.readFileSync('src/games/turbo-rider/render/ProjectionEngine.ts', 'utf8'); const lines = content.split('\\n'); console.log('Total lines:', lines.length); for (let i = 195; i < 220; i++) { console.log(i+1+': '+JSON.stringify(lines[i])); } )",
      "Shell(cd /home/ash/Desktop/Project/PartyPlay && node -e \" const fs = require('fs'); const content = fs.readFileSync('src/games/turbo-rider/render/ProjectionEngine.ts', 'utf8'); let depth = 0; let inStr = false; let strCh = ''; let line = 1; for (let i = 0; i < content.length; i++) { const c = content[i]; if (c === '\\n') line++; if (inStr) { if (c === strCh && content[i-1] !== '\\\\\\\\') inStr = false; continue; } if (c === '\\\"' || c === \\\"'\\\" || c === '\\`') { inStr = true; strCh = c; continue; } // crude comment skip if (c === '/' && content[i+1] === '/') { while (i < content.length && content[i] !== '\\n') i++; continue; } if (c === '{') depth++; if (c === '}') depth--; if (depth < 0) { console.log('Unbalanced } at line', line); break; } } console.log('Final brace depth at line', line, ':', depth); \")",
      "Shell(cd /home/ash/Desktop/Project/PartyPlay && cat > /tmp/check.js << 'EOF' const ts = require('typescript'); const fs = require('fs'); const src = fs.readFileSync('src/games/turbo-rider/render/ProjectionEngine.ts', 'utf8'); const sf = ts.createSourceFile('a.ts', src, ts.ScriptTarget.Latest, true); function walk(node, depth=0) { if (node.kind === ts.SyntaxKind.CloseBraceToken || node.kind === ts.SyntaxKind.OpenBraceToken) { console.log(' '.repeat(depth) + ts.SyntaxKind[node.kind] + ' @ ' + src.substring(node.pos, node.end)); } ts.forEachChild(node, c => walk(c, depth+1)); } walk(sf); EOF node /tmp/check.js 2>&1 | tail -10)",
      "Shell(cd /home/ash/Desktop/Project/PartyPlay && cat > /tmp/check.js << 'EOF' const ts = require('/home/ash/Desktop/Project/PartyPlay/node_modules/typescript'); const fs = require('fs'); const src = fs.readFileSync('src/games/turbo-rider/render/ProjectionEngine.ts', 'utf8'); const sf = ts.createSourceFile('a.ts', src, ts.ScriptTarget.Latest, true); function getLine(pos) { return src.substring(0, pos).split('\\n').length; } function walk(node) { const line = getLine(node.pos); if (node.kind === ts.SyntaxKind.Unknown) { console.log('Unknown @ line', line, ':', src.substring(node.pos, Math.min(node.end, node.pos+50))); } ts.forEachChild(node, walk); } walk(sf); console.log('Done'); EOF node /tmp/check.js 2>&1 | head -10)",
      "Shell(cd /home/ash/Desktop/Project/PartyPlay && cat > /tmp/check2.ts << 'EOF' import * as ts from 'typescript'; import * as fs from 'fs'; const src = fs.readFileSync('src/games/turbo-rider/render/ProjectionEngine.ts', 'utf8'); const sf = ts.createSourceFile('a.ts', src, ts.ScriptTarget.Latest, true); const diags = sf.parseDiagnostics || []; for (const d of diags) { const pos = d.start || 0; const line = src.substring(0, pos).split('\\n').length; const col = pos - src.lastIndexOf('\\n', pos - 1); console.log(`Line ${line} col ${col}: ${d.messageText}`); } EOF npx ts-node /tmp/check2.ts 2>&1 | head -10)",
      "Shell(npx tsc --noEmit 2>&1)",
      "Shell(npm:*)",
      "Shell(npx oxlint src/platform/stores/runAndGunStore.ts src/platform/components/SoldierPortrait.tsx src/platform/screens/run-and-gun/ 2>&1)",
      "Shell(npx tsc --noEmit 2 >& 1)",
      "Shell(grep:*)"
    ],
    "deny": [],
    "defaultMode": "auto-accept"
  }
}